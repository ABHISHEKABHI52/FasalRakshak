# FasalRakshak backend — production dependencies (Phase 1)
# Python 3.11+ (tested on 3.14); install with: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14
pydantic>=2.9
pydantic-settings>=2.6
PyJWT>=2.10
argon2-cffi>=23.1
asyncpg>=0.30
aiosqlite>=0.20
